import React, { Component } from 'react';
import ToDo from '../containers/to_do.jsx';

export default class App extends Component {
    render() {
        return (
            <div>
                <ToDo />
            </div>
        )
    }
}
